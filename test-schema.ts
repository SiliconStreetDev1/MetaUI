import { SchemaNormalizer } from './src/nz/co/siliconst/ui5/metaui/core/SchemaNormalizer.ts'; const data = { Employees: [{ Name: 'John' }] }; const rawSchema = { additionalProperties: true }; const inferred = (SchemaNormalizer as any).inferSchemaFromData(data); const normalized = SchemaNormalizer.normalize(rawSchema); const merged = (SchemaNormalizer as any).deepMergeProperties(inferred, normalized); console.log(JSON.stringify(merged, null, 2));
