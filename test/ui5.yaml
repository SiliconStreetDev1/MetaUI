specVersion: "3.0"
metadata:
  name: metaui-sandbox
type: application
server:
  customMiddleware:
    - name: ui5-middleware-servestatic
      afterMiddleware: compression
      mountPath: /resources
      configuration:
        debug: true
        rootPath: ../dist/resources
